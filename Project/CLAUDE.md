SongLinkr is a simple iOS utility application which takes a link to a song/album on one platform, and returns a list of links to the song on other platforms

There is also a Shazam feature which listens to and identifies the song first.

Networking lives in SongLinkrNetworkCore.

SongLinkr is written in Swift, using SwiftUI.
